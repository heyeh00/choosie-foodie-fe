
<!-- <view class="container">
    <view class="card-m1 {{item.selected ? 'selected' : 'not-selected'}}" wx:for="{{events}}" wx:for-item="event" bindtap="chosenRestaurant" data-restaurantid="{{event.restaurant.id}}" data-eventid="{{event.id}}">
        <view>{{event.restaurant.name}}</view>
        <view>{{event.restaurant.address}}</view>
        <view>Rating: {{event.restaurant.rating}}</view>
        <view>{{event.restaurant.ave_price}}¥/person</view>
    </view>
    <view class="our-button" bindtap="submitChoices">
        Submit
    </view>
</view> -->
    

<view wx:for="{{events}}" bindtap="chosenRestaurant" data-restaurantid="{{item.restaurant.id}}" data-eventid="{{item.id}}">
    <view class="card-m1 {{item.selected ? 'selected' : 'not-selected'}}">
    
        <!-- <image src="/icons/images/maotou.jpg" class="respic" mode="aspectFill"/> -->
        <!-- <image src="/icons/Food-Icons/{{item.restaurant.cuisine}}.png" style="width: 100rpx; height: 100rpx;" mode=""/> -->
        <view class="card-m2-info">
            <text style="font-size: 30rpx; margin-bottom: 30rpx;">{{item.restaurant.name}}</text>
            <view class="stars" style="display:flex">
                    <view>rating</view>
                    <view wx:for="{{item.restaurant.rating}}">
                        <image src="/icons/Page-icons/star_black.png" style="width: 20rpx; height: 20rpx;"></image>
                    </view>
                </view> 
            <view class="card-m2-info-line2">
                <view style="font-size: 24rpx;">￥{{item.restaurant.ave_price}}/person</view>
            </view>
        
            <view class="card-m2-info-line2">
                <view>
                    <image src="/icons/Food-Icons/cutlery.png" style="height: 30rpx; width:30rpx" mode="aspectFill"/>
                    <text >{{item.restaurant.cuisine}}</text>
                </view>
                <!-- <view>
                    <image src="/icons/Page-icons/pin_white.png" style="height: 30rpx; width:30rpx" mode="aspectFill"/>
                    <text>{{item.location}}</text>   
                </view> -->
            </view>
        
        </view>
    
    </view>
</view>

<!-- <button bind:chooseavatar="login" bindtap="submitChoices" open-type="chooseAvatar" class="our-button" style="margin-top: 100rpx">Submit</button> -->
