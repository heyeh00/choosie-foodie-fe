<!-- <view class="ma40 userinfo">
    <block wx:if="{{user.image_url}}">
        <image src="{{user.image_url}}" class="userinfo-avatar" mode="aspectFill" />
        <text>{{user.name}}</text>
    </block>
    <block wx:else>
        <image src="/icons/TabBar Icons/profile_white.png" class="userinfo-avatar" mode="aspectFill" />
        <button bindtap="login" type="primary" plain="true" class="mb50 login-btn">Login</button>
        <text>Somebody needs to login 🤔</text>
    </block>
</view> -->


<view class="bg" style="display: flex; justify-content: center;">
    <view wx:if="{{login}}">
        <view class="bgcard">
            <!-- <view wx:for="{{cards.s}}" wx:for-item="each card.s"> -->
            <view class="card.s" wx:for="{{restaurants}}">
                <image src="/icons/images/maotou.jpg">
                </image>
                <view class="card.s-infos">
                    <text style="font-size: 15rpx;">{{item.name}}</text>
                    <text style="font-size: 12rpx;">{{item.time}}</text>
                </view>
            </view>
        </view>
        <!-- </view> -->


        <view class="avatar">
            <image scr="/icons/images/panda.jpg">
            </image>
        </view>
    </view>

    <view wx:else>
        <view class="bgcard">
            <text style="margin-top: 50rpx; margin-bottom: 300rpx;">Somebody needs to login🤔</text>
            <button bindtap="login" class="our-button">Login</button>
        </view>
        <view class="avatar">
            <image src="/icons/TabBar-Icons/profile_white.png" class="userinfo-avatar"  mode="aspectFill"/>
        </view>
    </view>
</view>